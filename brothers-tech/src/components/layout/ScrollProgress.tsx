/**
 * Barra de progresso de scroll nativa (CSS animation-timeline).
 * Existe em markup; o CSS (styles/index.css) controla a animação via scroll.
 */
export function ScrollProgress() {
  return <div className="scrollProgress" aria-hidden="true" />;
}
